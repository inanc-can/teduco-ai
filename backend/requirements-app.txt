# Lightweight application dependencies - installed on each build
# These change more frequently and are quick to install

# --- Backend Web Framework / API ---
fastapi>=0.111
uvicorn[standard]>=0.29

sqlalchemy~=2.0          # ORM / SQL builder
psycopg[binary]~=3.1     # Postgres driver (binary = faster, no build tools needed)
psycopg2-binary~=2.9

python-multipart~=0.0.7  # enables FastAPI file-upload parsing
passlib[bcrypt]~=1.7     # easy/secure password hashing
supabase>=1.2  
python-dotenv>=1.0
pydantic>=2.0
pydantic[email]>=2.0
pydantic-settings>=2.0
python-jose[cryptography]>=3.3
requests>=2.30

# --- Document Parsing ---
pymupdf>=1.24            # lightweight PDF text extraction
